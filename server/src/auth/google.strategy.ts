import { Injectable } from '@nestjs/common';

@Injectable()
export class GoogleStrategy {
  // 🧠 Dummy class: bạn xác thực Google token trong AuthService rồi
  // nên file này chỉ để tránh lỗi import, không cần implement gì thêm
}
